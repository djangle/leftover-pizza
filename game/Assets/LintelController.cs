// Inspiration: A lintel or lintol is a type of beam that spans openings such as portals, doors, windows and fireplaces. It can be a decorative architectural element, or a combined ornamented structural item.

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LintelController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}



